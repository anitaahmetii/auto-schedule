export interface ImportHallModel {
    id?: string;
    name: string;
    capacity: number;
    userId?: string;
    userName?: string;
    location: string;  // si string sepse në Excel do të jetë emri i lokacionit
}